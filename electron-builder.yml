appId: com.lucy.app
productName: Lucy
copyright: Copyright © 2024

directories:
  output: dist
  buildResources: resources

files:
  - from: .
    filter:
      - package.json
      - app

extraMetadata:
  main: app/background.js

# Rebuild only better-sqlite3 for Electron (tree-sitter from swagger-ui
# runs in browser context and doesn't need Electron rebuild)
npmRebuild: true
nodeGypRebuild: false
nativeRebuilder: sequential
# Include native dependencies
asarUnpack:
  - "**/node_modules/better-sqlite3/**"

mac:
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  icon: resources/icon.icns
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: false

publish: null
