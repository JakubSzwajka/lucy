# OpenAPI Module

Central OpenAPI spec builder.

## Public API

- `openApiSpec` from `spec.ts`

## Use It Like This

Expose it from `/api/openapi` route and keep route-side formatting minimal.

## Responsibility Boundary

Defines API schema contract. Does not execute requests.
