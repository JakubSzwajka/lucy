# Legacy Memory Tool Module

Legacy Obsidian-backed memory tool exposed as `memory`.

## Tool Surface

Single tool name `memory` with actions:
- `save`
- `find`
- `update`

## Backend Capability

Combines Obsidian notes with conversation search for recall.

## Responsibility Boundary

Defines legacy tool behavior only.
Long-term structured memory behavior belongs to `continuity` module.
