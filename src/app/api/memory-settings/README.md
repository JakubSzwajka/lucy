# Memory Settings Routes

## Endpoints

- `GET /api/memory-settings`
- `PATCH /api/memory-settings`

## Purpose

User-level continuity tuning (scope, extraction model, context limits, question surfacing).
