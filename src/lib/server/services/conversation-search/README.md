# Conversation Search Module

Repository for cross-session conversation search with local context windows.

## Public API

- `getConversationSearchRepository()`
- `searchWithContext(query, userId, options)`

## Use It Like This

Used by integrations/tools that need historical context retrieval.
